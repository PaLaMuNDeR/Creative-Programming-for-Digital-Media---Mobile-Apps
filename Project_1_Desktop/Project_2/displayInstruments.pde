void displayInstruments()
{
  rectMode(CORNER);
  fill(0, 100);
  rect(0, 0, width, height/2);
  imageMode(CENTER);
  //X,Y for pics
//  int imgDrumsx = width/6;
//  int imgDrumsy = - 200 + height/2;
//  int imgDrums2x = width/6;
//  int imgDrums2y = - 400 + height/2;
//  int imgGuitarx = width/6;
//  int imgGuitary = - 600 + height/2;
//  int imgTambx = width/3 + width/2;
//  int imgTamby = - 200 + height/2;
//  int imgTrix = width/3 + width/2;
//  int imgTriy = - 400 + height/2;
//  int imgTrumpx = width/3 + width/2;
//  int imgTrumpy = - 600 + height/2;
 /* image (imgDrums, imgDrumsx, imgDrumsy, imgDrumsW, imgDrumsH);
  image (imgDrums2, imgDrums2x, imgDrums2y, imgDrums2W, imgDrums2H);
  image (imgGuitar, imgGuitarx, imgGuitary, imgGuitarW, imgGuitarH);
  image (imgTamb, imgTambx, imgTamby, imgTambW, imgTambH);
  image (imgTri, imgTrix, imgTriy, imgTriW, imgTriH);
  image (imgTrump, imgTrumpx, imgTrumpy, imgTrumpW, imgTrumpH);
  */
 
   
 
}
